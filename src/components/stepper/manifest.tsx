import { ComponentManifest, TypeProps } from 'yonui-extension'
const manifest: ComponentManifest = {
  name: 'stepper',
  label: 'label',
  type: TypeProps.BasicControls,
  props: [
  ],
  children: []
}

export default manifest
